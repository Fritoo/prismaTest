{
  "name": "test",
  "version": "1.0.0",
  "description": "Photo backup server to cdn",
  "main": "index.js",
  "scripts": {
    "all": "mysql.server start; mysql -u root < db.sql; npm install; npx prisma introspect; npx prisma generate; nodemon index.js"
  },
  "author": "Miles Alden",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^2.6.1",
    "bcrypt": "^5.0.0",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.4"
  }
}
